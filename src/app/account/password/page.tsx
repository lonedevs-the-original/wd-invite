"use client";
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";
import { useRouter } from "next/navigation";
export default function PasswordPage(){const router=useRouter();const [password,setPassword]=useState("");const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const {error}=await createClient().auth.updateUser({password});setBusy(false);if(error)setMessage(error.message);else{setMessage("Parol yangilandi.");setTimeout(()=>router.push("/admin"),800)}}return <main className="grid min-h-screen place-items-center bg-[#f5f0e9] px-5"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-7 shadow-xl"><h1 className="font-serif text-3xl">Yangi parol</h1><p className="mt-2 text-sm text-[#777169]">Kamida 8 belgidan iborat yangi parol kiriting.</p><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-6 w-full rounded-xl border px-4 py-3"/>{message&&<p className="mt-3 text-sm">{message}</p>}<button disabled={busy} className="mt-5 w-full rounded-xl bg-[#292823] py-3 font-semibold text-white">{busy?"Saqlanmoqda…":"Parolni o‘zgartirish"}</button></form></main>}
