import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
import { CreatorSettingsForm } from "@/components/creator-settings-form";
export default async function SettingsPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();const isAdmin=user?.app_metadata?.role==="admin";const {data:settings}=isAdmin?await supabase.from("site_settings").select("creator_name,card_number").eq("id","creator_support").single():{data:null};return <main className="px-5 py-10 sm:px-8 lg:px-12"><div className="mx-auto max-w-4xl"><p className="text-xs font-semibold uppercase tracking-[.18em] text-[#9b7a63]">Hisob</p><h1 className="mt-2 text-4xl font-semibold">Sozlamalar</h1><div className="mt-8 rounded-2xl border bg-white p-6"><p className="text-sm text-[#77736e]">Kirish emaili</p><p className="mt-1 font-medium">{user?.email}</p><Link href="/account/password" className="mt-5 inline-block rounded-xl border px-4 py-2.5 text-sm font-semibold">Parolni o‘zgartirish</Link></div>{isAdmin&&<><h2 className="mt-10 text-2xl font-semibold">Qo‘llab-quvvatlash ma’lumotlari</h2><p className="mt-2 text-sm text-[#77736e]">Bosh sahifa va dashboarddagi popup uchun.</p><CreatorSettingsForm name={settings?.creator_name??"Yaratuvchi"} card={settings?.card_number??""}/></>}</div></main>}
