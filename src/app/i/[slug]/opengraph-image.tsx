import { ImageResponse } from "next/og";
export const size={width:1200,height:630};export const contentType="image/png";export const alt="Wedding invitation";
export default async function Image({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const response=await fetch(`${process.env.NEXT_PUBLIC_SUPABASE_URL}/rest/v1/invitations?slug=eq.${encodeURIComponent(slug)}&select=partner_one,partner_two,event_date,venue&limit=1`,{headers:{apikey:process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!}});const [item]=await response.json();return new ImageResponse(<div style={{width:"100%",height:"100%",display:"flex",flexDirection:"column",alignItems:"center",justifyContent:"center",background:"#f8f4ed",color:"#302820",fontFamily:"serif"}}><div style={{fontSize:25,letterSpacing:8,textTransform:"uppercase",color:"#9b6a4b",display:"flex"}}>Wedding invitation</div><div style={{fontSize:92,marginTop:45,display:"flex"}}>{item?`${item.partner_one} & ${item.partner_two}`:"Taklif."}</div><div style={{fontSize:30,marginTop:35,color:"#766a61",display:"flex"}}>{item?`${String(item.event_date).split("-").reverse().join(".")} · ${item.venue}`:"A beautiful day awaits"}</div></div>,size)}
